import { EmailLinkPipe } from './email-link.pipe';

describe('EmailLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
